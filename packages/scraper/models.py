# from packages.core.db import ConnectionsDB


# class DemoModel():
#     """docstring for CtrlMod1"""

#     async def test(self):
#         query = "SELECT * FROM keywords_aws limit 10"
#         return await(await ConnectionsDB().get_connection("yaxaws")).select(query, 'all')
